# See backend-design.md §2 — supply-chain security policy.
# Install scripts were the exact vector in the 2026 keyv/cacheable compromise.
ignore-scripts=true
save-exact=true
# Every dependency is pinned; a range would let a hijacked patch arrive silently.
